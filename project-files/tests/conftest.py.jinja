"""Pytest configuration and shared fixtures for {{ project_name }} tests."""

import pytest


@pytest.fixture
def sample_fixture():
    """Example fixture that can be used across test modules."""
    return {"key": "value", "project": "{{ project_name }}"}